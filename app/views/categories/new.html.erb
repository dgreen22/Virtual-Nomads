<div class= "row">
	<div class= "col-md-3 col-md-offset-6">
		<%= link_to "See all Categories", categories_path, class: "btn btn-primary" %>
		<%= form_for @category do |c| %>
			<%= c.label :category_name %>
			<%= c.text_field :category_name %><br>

			<%= c.submit "Submit" %>
		<% end %>
	</div>
</div>